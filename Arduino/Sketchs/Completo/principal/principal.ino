#include "header.h"

void setup() {
  pinMode(lampada1, OUTPUT);
  pinMode(lampada1, OUTPUT);
  pinMode(lampada1, OUTPUT);
  pinMode(SPir, INPUT);
  pinMode(sCorrente, INPUT);
}

void loop() {
  // put your main code here, to run repeatedly:

}
